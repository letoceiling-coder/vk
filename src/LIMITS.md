# Лимиты и ограничения VK Bot API

Все классы автоматически валидируют данные перед отправкой в VK API.

## 📝 Текстовые ограничения

### Сообщения
- **Максимальная длина текста сообщения**: 4096 символов
- **Максимальное количество вложений**: 10
- **Максимальное количество пересылаемых сообщений**: 10

### Кнопки
- **Текст кнопки**: до 40 символов
- **Payload кнопки**: до 255 байт

## ⌨️ Клавиатура

### Лимиты клавиатуры
- **Максимум кнопок**: 40 (всего в клавиатуре)
- **Максимум рядов**: 10
- **Максимум кнопок в ряду**: 5

### Типы кнопок

1. **text** - Текстовая кнопка (4 цвета: primary, secondary, negative, positive)
2. **callback** - Callback кнопка (для inline клавиатур)
3. **open_link** - Открывает ссылку
4. **location** - Запрашивает геолокацию
5. **vkpay** - VK Pay кнопка
6. **open_app** - Открывает VK Mini App

### Цвета кнопок

- `primary` - Синяя кнопка
- `secondary` - Белая кнопка (по умолчанию)
- `negative` - Красная кнопка
- `positive` - Зелёная кнопка

## 📁 Файлы

- **Максимальный размер фотографии**: 50 MB
- **Максимальный размер документа**: 200 MB
- **Максимальный размер аудио**: 200 MB
- **Максимальный размер видео**: 2 GB

## ⚡ Rate Limits

- **Максимум запросов в секунду**: 20
- **Максимум сообщений в секунду**: 20
- **Максимум сообщений в день для одного пользователя**: 500

## 💾 Storage

- **Максимальная длина ключа**: 100 символов
- **Максимальная длина значения**: 4096 символов
- **Максимальное количество ключей**: 1000

## 🔧 Callback API

- **Максимальное количество серверов**: 10
- **Timeout для запросов**: 10 секунд

## 🚨 VK API Error Codes

- `5` - Неверный access_token
- `6` - Слишком много запросов в секунду
- `7` - Нет прав для выполнения действия
- `9` - Flood control (слишком много однотипных действий)
- `10` - Internal server error
- `14` - Требуется ввод кода с картинки (captcha)
- `100` - Один из обязательных параметров не был передан
- `113` - Неверный user_id
- `901` - Пользователь запретил отправку сообщений от сообщества
- `902` - Пользователь заблокирован
- `917` - Сообщение не может быть отправлено (privacy)

## ✅ Автоматическая валидация

Все данные автоматически проверяются:

```php
use App\VK\VK;
use App\VK\Exceptions\VKValidationException;

try {
    // Слишком длинный текст
    vk_send(123456789, str_repeat('A', 5000));
} catch (VKValidationException $e) {
    echo $e->getMessage();
    // "Message text is too long (5000 characters). Maximum: 4096"
}
```

## 🛠️ Вспомогательные методы

### Обрезка текста

```php
use App\VK\Validator;

$text = str_repeat('A', 5000);
$truncated = Validator::truncateText($text, 4096);
```

### Разбиение текста

```php
$longText = file_get_contents('long_article.txt');
$messages = Validator::splitLongText($longText);

foreach ($messages as $message) {
    vk_send(123456789, $message);
}
```

## 📋 Примеры валидации

### Валидация сообщения

```php
try {
    vk_send(123456789, ''); // ❌ Пустое сообщение (но VK разрешает, если есть attachment)
} catch (VKValidationException $e) {
    // Обработка ошибки
}

try {
    vk_send(123456789, str_repeat('A', 5000)); // ❌ Слишком длинный
} catch (VKValidationException $e) {
    // "Message text is too long (5000 characters). Maximum: 4096"
}
```

### Валидация клавиатуры

```php
$keyboard = vk_keyboard();

try {
    // Слишком много кнопок в ряду
    $keyboard->row();
    for ($i = 0; $i < 10; $i++) {
        $keyboard->text("Кнопка {$i}", "btn{$i}");
    }
    $keyboard->get(); // ❌ Ошибка
} catch (VKValidationException $e) {
    // "Too many buttons in row..."
}

try {
    // Слишком длинный текст на кнопке
    $keyboard->row()
        ->text(str_repeat('A', 50), 'btn'); // ❌ Ошибка
} catch (VKValidationException $e) {
    // "Button text is too long..."
}
```

### Валидация цвета кнопки

```php
try {
    vk_keyboard()
        ->row()
        ->text('Кнопка', 'btn', 'invalid_color'); // ❌ Ошибка
} catch (VKValidationException $e) {
    // "Invalid button color: invalid_color. Allowed: primary, secondary, negative, positive"
}

// ✅ Правильно
vk_keyboard()
    ->row()
    ->text('Кнопка', 'btn', 'primary');
```

### Валидация URL

```php
try {
    vk_keyboard()
        ->row()
        ->link('Сайт', 'invalid-url'); // ❌ Ошибка
} catch (VKValidationException $e) {
    // "Invalid URL: invalid-url"
}

// ✅ Правильно
vk_keyboard()
    ->row()
    ->link('Сайт', 'https://example.com');
```

## 💡 Рекомендации

### 1. Обрабатывайте исключения

```php
use App\VK\Exceptions\VKException;

try {
    vk_send($userId, $message);
} catch (VKException $e) {
    Log::error('VK API error: ' . $e->getMessage());
}
```

### 2. Используйте вспомогательные методы

```php
// Автоматическая обрезка
$text = Validator::truncateText($longText, 4096);
vk_send($userId, $text);

// Разбиение на несколько сообщений
$messages = Validator::splitLongText($longText);
foreach ($messages as $message) {
    vk_send($userId, $message);
}
```

### 3. Используйте очереди для массовых рассылок

```php
use App\Jobs\VK\SendBroadcastJob;

SendBroadcastJob::dispatch('Важное объявление');
```

### 4. Соблюдайте Rate Limits

```php
// НЕ ДЕЛАЙТЕ ТАК:
foreach ($users as $user) {
    vk_send($user->vk_id, 'Сообщение'); // Будет превышен лимит!
}

// ✅ ПРАВИЛЬНО - используйте очереди с задержками:
use App\Jobs\VK\SendMessageJob;

foreach ($users as $index => $user) {
    SendMessageJob::dispatch($user->vk_id, 'Сообщение')
        ->delay(now()->addSeconds($index * 0.05)); // 20 сообщений/сек
}
```

## 📖 Документация

- [README.md](README.md) - Основная документация
- [SETUP.md](SETUP.md) - Установка и настройка
- [FEATURES.md](FEATURES.md) - Все возможности
- [Официальная документация VK](https://dev.vk.com/api/bots)
- [Список ошибок VK API](https://dev.vk.com/reference/errors)

